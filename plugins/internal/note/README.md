# Note Plugin

This is a (currently WIP) clone of Ingen's internal note plugin.

It uses the same URI (http://drobilla.net/ns/ingen-internals#Note) which is a
bit naughty, but I guess most ingen synths will use it so it seems like the
sensible thing to do.
